tcp 接收消息 recv
recv 在自己这端的缓冲区为空时,会阻塞
tcp 有粘包现象,所谓的粘包问题主要还是因为接收方不知道消息之间的界限,不知道一次性提取多少字节数据造成的
tcp 内部算将短时内的几次小的数据包组合成一个包发送

udp 接收消息为 recvfrom
recvfrom 在自己这端的缓冲区为空时,不会阻塞
udp 没有粘包
udp 一个recvfrom 就对应一个sendto

